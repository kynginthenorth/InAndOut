
@{
    ViewData["Title"] = "Expense | Home";
}
@model IEnumerable<InAndOut.Models.ExpenseType>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Expense Type List</h2>
        </div>
        <div class="col-6 text-right">
            <a class="btn btn-primary" asp-controller="ExpenseType" asp-action="Create">Create new item</a>
        </div>
    </div>
    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        Expense Type Name
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var expense in Model)
                {
                    <tr>
                        <td width="35%">@expense.Name</td>
                        <td width="30%">
                            <div class="w-100 button-group" role="group">
                                <a class="btn btn-primary mx-1" asp-controller="ExpenseType" asp-route-Id="@expense.Id" asp-action="Update">Update</a>
                                <a class="btn btn-danger mx-1" asp-controller="ExpenseType" asp-route-Id="@expense.Id" asp-action="Delete">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No items created yet.</p>
    }
</div>
